# 旧 redbook UI 迁移说明

以下文件从只读参考仓库 `https://github.com/18762389735-dotcom/redbook` 迁入，并在本仓库独立使用：`AppShell`、`SidebarNav`、`PageHeader`、`StatusTag`、`DataCard`、`UiIcon`、`AccountHeader`、全局 CSS，以及 `HomePage`、`HotContentPage`、`KnowledgePage`、`TopicPlanPage`、`WritingPage` 的 UI 源文件。导航文字和入口已按 Batch 01 调整；旧项目 API、Agent job 轮询和单体 workspace 后端均未迁入。

旧仓库的许可证文本与 Beav 一样标为“MIT License – Non-Commercial Use Only”。本仓库在非商业范围内保留该迁移说明；如需再分发或商业使用，应先核实并取得旧仓库作者许可。
