import { formatDateTime } from "../utils/formatters";
import { UiIcon } from "./UiIcon";
export function AccountHeader({ profile, updatedAt, onNavigate }) { const positioning = profile?.positioning || profile?.niche || "尚未配置账号资料"; const name = profile?.displayName || "我的主账号"; const initial = name.slice(0, 1); return <header className="account-header"><div className="account-identity"><div className="account-avatar" aria-label="我的主账号头像">{initial}</div><div><div className="account-name">{name} {profile?.updatedAt ? <span aria-label="用户已确认">●</span> : null}</div><p>{positioning}<i />最近更新：{formatDateTime(updatedAt, "暂无更新")}</p></div></div><div className="account-actions"><button className="button button--secondary" onClick={() => onNavigate("account")}><UiIcon name="refresh" size={20} />账号设置</button><button className="button button--primary" onClick={() => onNavigate("discover")}><UiIcon name="search" size={20} />打开发现</button></div></header>; }
